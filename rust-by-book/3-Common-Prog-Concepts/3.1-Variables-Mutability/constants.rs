// constants are always immutable and cannot be made mutable
// declared using 'const' keyword, instead of let
// can be declared in any scope, including global scope
// must be set only to a constant expression, not the result of a value that could only be computed
// at runtime

const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;

// constants are valid for the entire time a program runs, within the scope they are declared in
